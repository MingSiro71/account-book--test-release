<%= form_with scope: :division, url: divisions_path do |f| %>
  <%= f.hidden_field :user_id, :value => session[:user_id] %>
  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>
  <%= f.submit "追加" %>
<% end %>