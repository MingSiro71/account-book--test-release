<% provide( :title, "#{@division.name}の編集" ) %>
<main class="col-sm-10 col-sm-offset-1">
<h1><%= @division.name %>の編集</h1>
<div class="col-sm-12">
  <%= form_with model: @division, method: :patch do |f| %>
    <%= f.label "事業領域名" %>
    <%= f.text_field :name %>
    <%= f.submit "名称変更", class: "btn btn-primary pull-right" %>
  <% end %>
  <br>
</div>
<div class="col-sm-12 danger-zone ">
  <strong>Danger Zone!</strong>
  <br>
  <%= link_to "削除する", division_path(@division), method: :delete, data: {confirm: "削除しますか?", commit: "削除する", cancel: "削除しない" }, class: 'btn btn-danger pull-right' %>
  <p>
    削除してしまうと、事業に紐づいているレコードは参照できなくなります。
  </p>
</div>
</main>